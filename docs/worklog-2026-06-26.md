# NAAS portal — worklog (session 2026-06-26 → 27)

Concept site = Astro static at `site/`, deployed to Cloudflare Pages `naas-portal-gravitas`.
All work committed to `main` and pushed to `origin` (Kovalsky/naas-concept).

## ✅ Done this session

### UI/UX review of the existing site + fixes
- Accessibility/quality pass. Fixed: gold-on-light text contrast → added `--gold-ink #8A6508` (~5:1); `prefers-reduced-motion` handling; sticky footer on short pages; visible focus ring on search; 44×44 header touch targets; removed leaked dev text on the Statute page; unified placeholder radius.

### Hero redesign + variant switcher
- Built **3 hero variants**: Classic (original centered), **Emblem** (asymmetric, AI navy engraved medallion), **Banner** (full-bleed engraved field).
- Generated the medallion + wide banner with **Gemini** (`gemini-3-pro-image`), optimized to **WebP** (emblem 603→94KB, banner 967→169KB), responsive `<picture>`/`image-set`, frameless banner crop.
- **Switcher** in the utility bar (top-left): Класична · Емблема · Банер, 3-option selector, persisted in `localStorage`, default = Класична (live look unchanged), mobile-safe.
- Polish passes (multi-agent, adversarially verified): mobile scrim contrast, focus-visible rings, centered+balanced stats band, squared medallion (even margins), divider alignment, label tracking.

### Content-depth audit (vs the ТЗ, not the old site)
- Established the **ТЗ governs scope** (`docs/NAAS_TZ_v9 (lite).docx`). Site is well-populated; ТЗ-required-but-empty sections are **intentional placeholders** (do not delete).

### Resource sections migrated from the old site → native pages
- **/video** — gallery (self-hosted thumbnails, play overlay → YouTube).
- **/agrolectures** — 20 video-lectures (all live).
- **/e-biblioteka** — catalog of **40 periodicals**, alphabetised, client-side search filter.
- Data extracted from the live HTTP-only old site (cp1251) into `site/src/data/{video,agrolectures,elibrary}.json`. Homepage resource cards + footer + search now point **internal**.

### Design-bug verification of the new pages (multi-agent, adversarially verified)
- Fixed: card grids overflowed on <340px phones (bare `minmax` → wrapped in `min(...,100%)`, a regression of the documented global.css fix); E-Library search input 15→16px (kills iOS Safari focus auto-zoom); filter now announces results to screen readers (`aria-live` on count + empty message); stronger search focus ring; E-Library cards top-aligned so long titles don't float the icon; Video gallery marked up as `<ul>/<li>`; new-tab links now announce "(відкривається у новій вкладці)"; 2-card Video grid centered + width-capped so it reads as intentional.
- Video data: dropped the 5 dead YouTube clips (deleted/private), kept the 2 live; cleaned a title with extraction junk.
- Deferred (low/cosmetic): Agrolectures title line-clamp; self-hosting thumbnails (you reverted to i.ytimg.com — kept; revisit for the ТЗ "no external CDN" rule).

### Infra / housekeeping
- Gemini image-gen toolchain (key in `~/.naas_gemini.env`, venv, `generate.py`).
- Deployed to `naas-portal-gravitas` (commit + push each iteration).
- Memory updated: ТЗ authority, hero+Gemini setup, UI-review tooling.

## ⏳ To adjust / ship later / work deeper

### 🔴 Critical — stakeholder corrections (Патика, separate branch, NOT deployed)
- **Founding year 1918, not 1931** — appears across the site (homepage facts, header «ЗАСН. 1931», emblem «EST. 1931»). Needs a coordinated fix incl. regenerating/relabelling the emblem.
- **176 наукових установ, not 50** — homepage stat + `lib/data` divisions.
- **«вища галузева» wording rejected** — verify final hero/sub copy everywhere.
- Sources: `docs/source-of-truth-naas.md`, `docs/feedback-2026-06-patyka.md`. Do on a branch.

### Resource pages
- **E-Library depth:** currently catalog-only (40 periodicals → link to old archive). Deepen to per-periodical **issues/PDFs** (40 × many issues) — needs a crawl + hosting decision.
- **/video:** only **2 of the old site's 7** clips are still live on YouTube (5 deleted/private). Re-source the 5 or reconsider the section.
- Thumbnails are self-hosted (good, satisfies ТЗ "no external CDN"); add a refresh process for production.
- **4th resource site** unidentified — ТЗ says «посилання на 4 сайти»; we have 3 (E-Library, Video, Agrolectures).

### ТЗ-required but pending stakeholder content (keep as placeholders)
- Запобігання корупції, Рішення та Постанови Президії, Новини відділень, Президія bios/photos.

### Built but thinner than ТЗ demands
- **Атестація:** ТЗ wants licenses, ОНП/навчальні плани, admission rules, cost, dissertation texts/автореферати, fahові-journals registry (МОН А/Б), держатестація — currently an 8-doc list.
- **Структура:** ТЗ wants керівник/фото/лого per institute (currently blank).

### Next refinement candidates (ranked earlier)
1. Interior-page design pass: struktura, novyny, naan-sohodni, kontakty.
2. News breadth — expand beyond the 9-item slice (extractor exists in `naas_news_slice/`).

### Architecture note
- ТЗ specs **WordPress 6.5 / PHP / MySQL / multi-level CMS (per-department microsites) / 2FA / WAF / PageSpeed ≥90 / WCAG AA**. This repo is the **Astro design concept**, not the final CMS build.

## Open questions for you
- 4th resource site name?
- E-Library: catalog-only for now, or invest in per-issue migration?
- Should production deploys continue while the 1918/176 corrections are pending on a branch?
