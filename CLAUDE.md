# NAAS — правила проєкту для Claude

## Живий сайт `naas.gov.ua` (стара CMS)

Факти (перевірено 2026-07 через `curl` + адмінку):

- CMS: **1С-Bitrix** (Nginx/PHP). Хостинг Internet Invest (Mirohost), IP `77.87.194.92`.
- Канонічний протокол — **HTTP**, кодування сторінок **windows-1251**.
- **HTTPS зламаний** — self-signed сертифікат. Для будь-яких запитів вказуй `http://` **явно** (інакше редірект на HTTPS впирається в помилку сертифіката й вкладка «висить»).
- `/sitemap.xml` → **404** (XML-мапи немає). `robots.txt` — застарілий друпалівський (залишок від попередньої CMS).
- Адмінка: `http://naas.gov.ua/bitrix/admin/`.

## Робота з адмінкою Bitrix — **ТІЛЬКИ ЧИТАННЯ**

Доступ — через розширення **claude-in-chrome** (не headless):

1. Користувач логіниться сам у своєму Chrome. Токен авторизації Bitrix
   (`BITRIX_SM_UIDH` / `PHPSESSID`) — **`HttpOnly`**, скриптом його не дістати (і не треба).
2. Працюй через `mcp__claude-in-chrome__javascript_tool` у вкладці адмінки.
   In-page `fetch()` виконується в origin сайту й **автоматично несе `HttpOnly`-cookie**
   → авторизований READ-доступ **без** витягування токена.
3. **Не** роби скріншоти й кліки для навігації — це повільно. Читай DOM і роби
   `GET`-`fetch` на сторінки-звіти, парсь HTML, повертай **лише агреговані числа**
   (не URL, не куки).

### Дисципліна read-only (жорстко)

- Тільки `GET` і читання DOM. **Ніяких `POST`.**
- **Ніколи** не відкривай URL з мутуючими параметрами:
  `action=delete|save|apply|activate|deactivate`, `&sessid=…`, переіндексація, очищення кешу.
- Ніяких кнопок «Зберегти / Застосувати / Видалити / Очистити кеш / Переіндексувати».
- Будь-яка дія, що змінює стан → **спершу спитати користувача**.

### Особливості середовища

- Дашборд адмінки вантажиться повільно → таймаути `document_idle` (~45 с) — це нормально, повторити.
- Навігація — завжди явний `http://…`.
- MCP-шар маскує query-string і «sensitive keys» (`[BLOCKED: …]`) — це очікувано;
  діставай дані in-page і повертай лише результат.

### Чому НЕ headless-curl через cookie

Auth-cookie `HttpOnly` (скрипт його не читає), а ручне копіювання токена = експозиція
**живого адмін-токена продакшн `.gov.ua`** у plaintext/логах. In-tab JS дає те саме без ризику.

---

<!-- GSD-managed sections below (generated by /gsd:new-project 2026-07-02; regenerate via gsd-sdk generate-claude-md). The NAAS rules above are hand-written — do not remove them. -->

<!-- GSD:project-start source:PROJECT.md -->
## Project

**NAAS Portal Program (new naas.gov.ua)**

A program to replace the old 1C-Bitrix site of the National Academy of Agrarian Sciences of Ukraine (naas.gov.ua) with a new web presence, in two lines of work: (a) a static **showcase** of the stakeholder-approved Modern design on `new.naas.gov.ua` (Mirohost, static FTP deploy), and (b) a full **portal** — Next.js frontend + Directus/MySQL backend on the Mirohost eVPS — that serves ALL legacy content 1:1 at the exact old URLs, ready for a zero-redirect domain swap.

GSD orchestrates four pre-existing tracks defined in `docs/superpowers/ROADMAP.md`. The detailed superpowers implementation plans are the **source of truth for execution steps** (user-stated trust ~99%; spot-check, don't re-derive). Plans are owned by their own sessions — **editing another session's plan documents is forbidden**; cross-track decisions go through the ROADMAP or the user.

**Core Value:** The old site keeps running untouched until the new portal serves all of its public content at the same URLs — SEO weight survives the eventual domain swap 1:1 (zero redirects for migrated pages).

### Constraints

- **Legal**: gov.ua domains must be hosted in Ukraine — Mirohost only for `*.naas.gov.ua`; no foreign CDN — law, non-negotiable.
- **Access discipline**: ask the user before EVERY SSH/FTP connection to Mirohost; Bitrix admin and hosting are read-only (single sanctioned mutation: the Track 1 banner href, behind an explicit user gate) — production .gov.ua safety.
- **Server**: Mirohost eVPS-8, 2 vCPU / 4 GB, no root, no compiler, Node 20.20.2 ceiling, MySQL only (no Postgres/Redis), crontab blocked → systemd units via Mirohost support, nginx proxy via panel.
- **Budget**: fixed — hosting already paid; the old site runs in parallel until the swap.
- **Process**: every commit pushed to `origin` `main` (github.com:Kovalsky/naas-concept) — but respect parallel-session interleaving; never stage other sessions' files; all documentation in English, chat in Ukrainian.
- **Doc ownership**: superpowers plans/specs belong to their owning sessions — GSD reads them, never edits them.
<!-- GSD:project-end -->

<!-- GSD:stack-start source:STACK.md -->
## Technology Stack

Technology stack not yet documented. Will populate after codebase mapping or first phase.
<!-- GSD:stack-end -->

<!-- GSD:conventions-start source:CONVENTIONS.md -->
## Conventions

Conventions not yet established. Will populate as patterns emerge during development.
<!-- GSD:conventions-end -->

<!-- GSD:architecture-start source:ARCHITECTURE.md -->
## Architecture

Architecture not yet mapped in `.planning/codebase/`. Until then, `docs/architecture/portal-architecture.md` and `docs/infrastructure/mirohost-server.md` are the authoritative architecture/infrastructure references.
<!-- GSD:architecture-end -->

<!-- GSD:skills-start source:skills/ -->
## Project Skills

No project skills found. Add skills to any of: `.claude/skills/`, `.agents/skills/`, `.cursor/skills/`, `.github/skills/`, or `.codex/skills/` with a `SKILL.md` index file.
<!-- GSD:skills-end -->

<!-- GSD:workflow-start source:GSD defaults -->
## GSD Workflow Enforcement

Before using Edit, Write, or other file-changing tools, start work through a GSD command so planning artifacts and execution context stay in sync.

Use these entry points:
- `/gsd-quick` for small fixes, doc updates, and ad-hoc tasks
- `/gsd-debug` for investigation and bug fixing
- `/gsd-execute-phase` for planned phase work

Do not make direct repo edits outside a GSD workflow unless the user explicitly asks to bypass it.

**Scope note:** this enforcement applies to GSD sessions in this checkout. Parallel non-GSD sessions executing their own superpowers plans (see doc-ownership rule above) are exempt.
<!-- GSD:workflow-end -->

<!-- GSD:profile-start -->
## Developer Profile

> Profile not yet configured. Run `/gsd-profile-user` to generate your developer profile.
> This section is managed by `generate-claude-profile` -- do not edit manually.
<!-- GSD:profile-end -->
