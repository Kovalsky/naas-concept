// site-light now ships native E-Бібліотека / Відео / AGROLECTURES pages (same routes
// as the heavy build), so the shared internal routes resolve here — no external remap.
export {
  ROUTES, NAV, EXTERNAL, PARTNERS,
  SEARCH_INDEX, SHOWCASE, FOOTER, PLACEHOLDER_TEXT,
} from '@content/site';
