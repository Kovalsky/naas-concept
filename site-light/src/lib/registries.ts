// Re-export the shared document registries (single source of truth).
export { registries, byRoute, LEADS } from '@shared/lib/registries';
export type { Registry, RegItem } from '@shared/lib/registries';
