{
  "name": "naas-portal",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "wrangler pages deploy dist --project-name naas-portal-new"
  },
  "dependencies": {
    "astro": "^5.6.1"
  },
  "devDependencies": {
    "wrangler": "^3.114.1"
  }
}
